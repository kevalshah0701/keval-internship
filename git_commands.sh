For new branch
    -> new branch create => git checkout -b "dev/index-file-change"
    -> alredy exist => git checkout "branch-name"
Git Status
    -> git status
Git branch list
    -> git branch


=> after create new branch and changes on file need to do this steps
    -> git status
    -> git add .
    -> git commit -m "description"
    -> git push origin "branch-name"

    -> git status
    -> move in main or master branch => git checkout "branch-name"
    -> git pull origin master

-> conflict
    -> on current branch pull master branch code
    -> git pull origin master 
    -> git status
    -> git add .
    -> git commit -m "description"
    -> git push origin "branch-name"

-> pull request
    -> example pull request url => https://github.com/deepahir/learning/pull/1

